export const MemoDetail = ({ route }: any) => {
  const { id } = route.params;
  return <></>;
};
{
  /* <Pressable>수정</Pressable>
<Pressable onPress={() => handleConfirm("accept", el.id)}>
  Delete
</Pressable>
<View
  style={
    accept === "accept" ? styles.deleteModal : styles.deleteBasic
  }
>
  <Pressable onPress={() => handleDeleteClick(el.id)}>삭제</Pressable>
  <Pressable onPress={() => handleConfirm("", el.id)}>취소</Pressable>
</View> */
}
